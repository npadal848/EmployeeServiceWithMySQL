spring:
  datasource:
    driver-class-name: org.h2.driver
    url: jdbc:h2:~/test
    username: sa
    password: 
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none

